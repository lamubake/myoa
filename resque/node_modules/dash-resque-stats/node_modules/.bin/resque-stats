#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../node-resque-stats/bin/resque-stats" "$@"
  ret=$?
else 
  node  "$basedir/../node-resque-stats/bin/resque-stats" "$@"
  ret=$?
fi
exit $ret
